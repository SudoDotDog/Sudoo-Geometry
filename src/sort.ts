/**
 * @author WMXPY
 * @namespace Geometry
 * @description Sort
 */

import { Coordinate } from "./declare";

export const sortCoordinateByLinearDistance = (coordinates: Coordinate[]): Coordinate[] => {

    const clone: Coordinate[] = [...coordinates];

    return clone;
};
